package dbsp

// Old rule implementations (hashModTableRule, hashModDbRule, rangeDbRule,
// directDbRule, customDbRule, customTableRule) have been removed.
// All sharding rules are now expression-based.
// See sharding_expr_rule.go for the new implementations.
